# Dependencias del agente multimodal de triaje de incidentes.
# Probado con Python 3.12 (entorno virtual creado con uv o venv).

# Orquestacion (create_agent requiere LangChain 1.x)
langchain>=1.0.0
langchain-core>=1.0.0
langchain-ollama>=1.0.0
langchain-community>=0.4.0

# Interfaz
streamlit>=1.36.0

# Preprocesamiento de documentos
pymupdf>=1.24.0        # lectura de PDF (modulo fitz)
pillow>=10.3.0         # manejo de imagenes
pandas>=2.2.0          # lectura y analisis de CSV

# Utilidades
requests>=2.32.0       # comprobacion del servidor de Ollama
